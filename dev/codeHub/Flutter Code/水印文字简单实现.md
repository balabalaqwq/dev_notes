# 文字水印



[DartPad](https://dartpad.dev/ede301fabd27ca8e3b34ce60ab65fbc7?null_safety=true)



![image-20210517165640691](https://gitee.com/balabalaqwq/pics/raw/master/image-20210517165640691.png)



```
import 'dart:math';
import 'dart:ui';

import 'package:flutter/material.dart';

class Watermark extends StatelessWidget {
  final String str;

  final Widget child;

  Watermark({this.str, this.child});

  @override
  Widget build(BuildContext context) {
    if (this.str == null) return this.child;
    final watermarkWiget = Container(
      height: 40,
      child: Transform.rotate(
          angle:
          -pi / 6,
          child: Text(this.str, style: TextStyle(color: Color(0x20909090)))),
    );
    return Stack(
      children: [
        this.child,
        Positioned.fill(
          child: LayoutBuilder(
            builder: (context, constraints) {
              return BackdropFilter(
                filter: ImageFilter.blur(sigmaX: 0.1, sigmaY: 0.1),
                child: Container(
                  color: Colors.transparent,
                  child: Row(
                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      Expanded(
                        child: Column(
                          mainAxisAlignment: MainAxisAlignment.spaceBetween,
                          children: List.filled(constraints.maxHeight~/120, watermarkWiget),
                        ),
                      ),
                      Expanded(
                        child: Column(
                          mainAxisAlignment: MainAxisAlignment.spaceBetween,
                          children: List.filled(constraints.maxHeight~/120, watermarkWiget),
                        ),
                      ),
                    ]
              ),
                ),
              );
            },
          ),
        ),
      ],
    );
  }
}
```

